services:
  worker:
    build:
      context: .
      args:
        - BASE_IMAGE
    command: maestro-server --worker /worker/worker.py --port 8000 --reload True
    ports:
      - "8000:8000"
    expose:
      - "8000"
    volumes:
      - ./:/worker/
